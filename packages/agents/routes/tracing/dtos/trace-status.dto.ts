export type TraceStatus = {
  configured: bool,
  active: bool,
  backend: string,
  endpoint: string,
  publicKey: string,
  serviceName: string,
  environment: string,
  enabled: bool,
  secretStored: bool,
};
