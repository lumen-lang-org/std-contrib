// The shapes the card-plugins routes read and write.

export type CardInput = { toolName: string, marker: string, payload: string, hint: string };

export type CaseInput = { when: string, then: string };
