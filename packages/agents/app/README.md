# Moved

The Joule console now lives in its own repository:
https://github.com/joule-sh/console

This directory is kept only so stale paths fail with an explanation instead
of silently. The engine (everything else under packages/agents) is
unaffected and still lives here.
